import { useEffect,useState } from 'react'
import { ArrowLeft,Brain,ClipboardCheck,GraduationCap,LogOut,TrendingUp } from 'lucide-react'
import { collection,getDocs,query,where } from 'firebase/firestore'
import { db } from '../firebase'
export default function RoleDashboard({user,onBack}){const[records,setRecords]=useState({marks:[],attendance:[]}),[error,setError]=useState('');useEffect(()=>{if(user?.role!=='student')return;Promise.all([getDocs(query(collection(db,'marks'),where('studentId','==',user.uid))),getDocs(query(collection(db,'attendance'),where('studentId','==',user.uid)))]).then(([m,a])=>setRecords({marks:m.docs.map(x=>x.data()),attendance:a.docs.map(x=>x.data())})).catch(e=>setError(e.message))},[user]);const avg=m=>m.length?(m.reduce((a,x)=>a+Number(x.value||0),0)/m.length).toFixed(1):'—';const isStudent=user?.role==='student';return <main className="min-h-screen bg-slate-950 text-white"><nav className="border-b border-slate-800 bg-slate-900 p-5"><div className="mx-auto flex max-w-7xl items-center justify-between"><div><h1 className="font-bold">{isStudent?'Student Dashboard':'Teacher Dashboard'}</h1><p className="text-xs text-slate-500">{user?.name||user?.email}</p></div><button onClick={onBack} className="inline-flex items-center gap-2 rounded-lg border border-slate-700 px-3 py-2"><ArrowLeft size={16}/>Back</button></div></nav><section className="mx-auto max-w-6xl p-6">{error&&<p className="mb-5 rounded-lg bg-red-500/10 p-3 text-sm text-red-300">{error}</p>}{isStudent?<><div className="grid gap-4 sm:grid-cols-3"><Card label="Average Marks" value={`${avg(records.marks)}%`} icon={<GraduationCap/>}/><Card label="Attendance" value={`${avg(records.attendance)}%`} icon={<ClipboardCheck/>}/><Card label="Assessments" value={records.marks.length} icon={<TrendingUp/>}/></div><div className="mt-6 rounded-2xl border border-slate-800 bg-slate-900 p-6"><div className="flex items-center gap-2 text-emerald-400"><Brain/><h2 className="font-semibold">Academic Insight</h2></div><p className="mt-4 text-slate-400">Your dashboard summarizes the academic records currently stored for your student account. Risk prediction is generated from attendance, marks, recent trend and missed assessments.</p></div></>:<div className="rounded-2xl border border-slate-800 bg-slate-900 p-8"><h2 className="text-xl font-semibold">Teacher workspace</h2><p className="mt-2 text-slate-400">Use Attendance & Marks to record student performance and generate AI risk predictions.</p></div>}</section></main>}
function Card({label,value,icon}){return <div className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="flex items-center gap-3 text-emerald-400">{icon}<span className="text-sm text-slate-400">{label}</span></div><p className="mt-3 text-3xl font-bold">{value}</p></div>}
